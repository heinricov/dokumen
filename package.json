{
  "name": "dokumen",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "security:audit": "pnpm security:eslint && pnpm security:static",
    "security:eslint": "pnpm --filter backend exec eslint .",
    "security:static": "node scripts/security/static-audit.mjs"
  },
  "devDependencies": {
    "eslint-plugin-security": "^4.0.1",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.18",
    "typescript": "~6"
  },
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=20"
  }
}
